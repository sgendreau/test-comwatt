<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}rEARding!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="my-auto w-full">
        <div class="min-h-screen w-full bg-gray-100">
            <nav
                    class="font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full">
                <div class="mb-2 sm:mb-0">
                    <a href="{{ path('admin_index') }}" class="text-2xl no-underline text-grey-darkest hover:text-blue-dark">Admin rEARding</a>
                </div>
                <div class="flex">
                    <a href="{{ path('react') }}"
                          class="text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2">
                    Retour au Front
                    </a>
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}"
                              class="text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2">
                        Déconnexion
                        </a>
                    {% endif %}
                </div>
            </nav>
            <div class="w-full">
                <div class="container mx-auto mt-1 bg-white">
                    {% block body %}{% endblock %}
                </div>
            </div>
        </div>
    </body>
</html>
